trigger:
- dev

pool:
  vmImage: 'ubuntu-latest'
strategy:
  matrix:
    Python36:
      python.version: '3.6'
    Python37:
      python.version: '3.7'

steps:
- task: UsePythonVersion@0
  inputs:
    versionSpec: '$(python.version)'
  displayName: 'Use Python $(python.version)'

- script: |
    python -m pip install --upgrade pip
    pip install -e ".[all]"
  displayName: 'Install all dependencies'

- script:
    sphinx-apidoc -o api/internal ../dataprov
    make html
  workingDirectory: docs
  displayName: 'Build documentation'

#- script:
#    conda env create --quiet --file environment.yml
#  displayName: 'Create conda environment for integration tests'

#- script:
#    conda env create --quiet --file environment.yml
#    dataprov -i examples/bwa/genome.fa -o examples/bwa/genome.fa.bwt run bwa index examples/bwa/genome.fa
#  displayName: 'indexExample'